// for user context (a big context) 
import React from "react";


// create a context
const UserContext = React.createContext();


// this is a global but where is the data.
// this is step 1, this should be a js file!!
export default UserContext